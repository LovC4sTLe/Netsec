version: '3.9'

services:
  authen-service:
    restart: always
    container_name: authen-service
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 5000:5000
    volumes:
      - .:/authen_service